# Generated by `xcodegen generate` — never commit these.
*.xcodeproj
*.xcworkspace
MenuLens/Info.plist

# Build artifacts
DerivedData/
build/
*.ipa
*.dSYM.zip

# User state
xcuserdata/
*.xcuserstate
.DS_Store

# Secrets — never commit API keys
*.secret
.env

# Review-only secret, never commit
docs/review-key.local.md

# EU DSA trader 信息（含住址），never commit
docs/trader-info.local.md
